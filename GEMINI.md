# GEMINI.md — OmniMind

@AGENTS.md

`AGENTS.md` is the single canonical instructions file for this repo (shared with Claude Code
and Codex). This file exists only so Antigravity/Gemini CLI auto-loads it via import.

**If you're reading this as raw text** (i.e. the line above literally shows `@AGENTS.md`
instead of the project's actual status/tech-stack/rules content), the import didn't resolve
in this tool — open `AGENTS.md` directly instead of relying on this file. Either way, edit
`AGENTS.md`, not this file: forking content here is exactly the drift that caused the
2026-08-28 reconciliation between `CLAUDE.md`/`GEMINI.md`/`AGENTS.md` in the first place.
