"""Conversation memory: the persistent half of OmniMind's memory architecture.

See agents/memory/service.py (sliding window + rolling summary, per-thread) and
retrieval/memory_store.py (episodic recall across threads).
"""
