# CLAUDE.md — OmniMind

@AGENTS.md

`AGENTS.md` is the single canonical instructions file for this repo (shared with Codex and
Antigravity). This file exists only so Claude Code auto-loads it via import. Edit
`AGENTS.md`, not this file — a fork here is exactly the drift that caused the
2026-08-28 reconciliation between `CLAUDE.md`/`GEMINI.md`/`AGENTS.md` in the first place.
